You are refactoring an existing FastAPI backend (Python) for MikeAI. The endpoint /ai/meals/generate sometimes “drifts” (wrong cuisines), misses daily calories, and repeats dishes too soon. Fix it with strict validation, retries, and budget guardrails.

Context (do NOT change these external contracts)

Request body (Pydantic v2 names may differ in code):

{
  "profile": {
    "user_id": "u123",
    "daily_calorie_goal": 2100,
    "preferred_cuisines": ["Indian"],
    "diet_type": "none"
  },
  "days": 7,
  "mealsPerDay": 5,
  "enforceCuisine": true,
  "cuisineWhitelist": ["Indian"],
  "minGapDays": 2,
  "enforceCalories": true,
  "calorieTolerancePct": 0.08,
  "maxRebalanceAttempts": 2,
  "provider": "openai",
  "model": "gpt-4.1-mini"  // or leave null to auto
}


Response:

{
  "success": true,
  "plan": {
    "days": [ { "date": "YYYY-MM-DD", "meals": [ ... ], "total_calories": 2092 } ],
    "metadata": { ... }
  },
  "received": { ... echo of request ... }
}

Required changes

Determinism & Budget

Default temperature=0.2, top_p=0.9, max_tokens conservative (e.g., 800–1200 per call).

Add timeouts (15s) and retries with jitter (max 2) on 429/5xx.

Add a per-request budget cap (env: MIKE_AI_MAX_TOKENS=5000) and track consumed tokens; abort with 429 if exceeded.

Add caching: for the same (profile_hash, day, slots, cuisines) within 24h, reuse result (use a simple sqlite/redis or on-disk JSON cache; pick simplest available here).

Output validation (critical)

Validate LLM JSON with Pydantic against this schema:

class MealMacros(BaseModel):
    protein: float
    carbs: float
    fat: float

class Meal(BaseModel):
    slot: Literal["breakfast","lunch","dinner","snack1","snack2"]
    title: constr(strip_whitespace=True, min_length=3)
    calories: conint(ge=0, le=1500)
    macros: MealMacros
    ingredients: conlist(str, min_items=1)
    instructions: constr(min_length=10)
    cuisine: Optional[str] = None
    prep_time_min: Optional[int] = None

class DayPlan(BaseModel):
    date: constr(regex=r"^\d{4}-\d{2}-\d{2}$")
    meals: conlist(Meal, min_items=3, max_items=5)
    total_calories: Optional[int] = None


If validation fails, auto-repair by asking the model to regenerate only the failing fields (NOT the whole day).

Cuisine enforcement

Compute cuisines = body.cuisineWhitelist or body.profile.preferred_cuisines or ["Indian"].

For each meal: if cuisine not in whitelist AND title doesn’t clearly imply that cuisine, regenerate that single meal with constraints { enforce_cuisine: true, cuisine_whitelist: cuisines }.

Pass the cuisine constraints in the prompt and as structured control variables.

Calorie control

Let target = daily_calorie_goal, tolerance ±pct (8% default).

If day total outside band, regenerate one flexible slot (order: snack2 → snack1 → lunch → dinner → breakfast) with constraints:
{ target_total_calories, current_total_calories, cuisine_whitelist, avoid_titles }.

Max maxRebalanceAttempts rebalances per day.

Anti-repeat with minimum gap

Maintain a dict last_seen_idx[normalized_title] = day_index.

After drafting a day (in parallel), do a cross-day check under a lock:

If a title is seen too soon (day_idx - last_seen_idx[title] < minGapDays) or in avoidTitlesGlobal, regenerate that single meal with constraints { min_gap_days, last_seen_titles, avoid_titles, cuisine_whitelist }.

Normalize titles: lowercase, remove punctuation, collapse whitespace.

Concurrency & safety

Limit concurrency to 3 simultaneous day generations via asyncio.Semaphore(3).

Use an asyncio.Lock() for cross-day finalization of titles to prevent race duplicates.

Add server logging: log the received payload and a compact summary of actions (repairs, calorie fix, cuisine fixes, total tokens).

LLM prompt design

System prompt: “You are a nutrition coach. Output ONLY strict JSON matching the provided schema. Cuisine must be one of: {cuisine_whitelist}. Respect meal slots: {slots}. Prefer common Indian dishes when cuisine requires it.”

User prompt: include profile summary, calorie target, slots, avoid_titles, cuisine_whitelist, and explicit examples (2–3 Indian meals per slot).

Enforce “no commentary” and “no markdown”—JSON only.

Config via env

OPENAI_API_KEY

MIKE_AI_MAX_TOKENS=5000

MIKE_AI_TIMEOUT_SECS=15

MIKE_AI_DEFAULT_MODEL=gpt-4.1-mini (but allow override per request)

Tests (create tests/test_meal_generation.py)

Test 1: preferred_cuisines=["Indian"] → all meals pass _is_allowed_cuisine.

Test 2: daily_calorie_goal=2100 → day total within ±8%.

Test 3: minGapDays=2 → if Monday “Masala Oats” appears again on Tuesday, it must be replaced; Thursday is okay.

Test 4: Model returns junk → validator catches and triggers auto-repair; final output valid.

Mock network to be deterministic; include a “fake LLM” path toggled by MIKE_AI_FAKE=1.

Developer ergonomics

Add a ?dryRun=1 query flag to return only the planned instructions without calling the LLM (for debugging).

Return received echo in response for quick frontend verification.

Deliverables

Updated FastAPI route and helpers (prefer minimal surgery; keep existing models where possible).

A small llm_client.py update: retries, timeouts, token budget, deterministic params, cache layer.

Unit tests above, runnable with pytest -q.

README snippet explaining env vars and how budget guard works.

Important: Keep responses JSON-only from the model. Validate every model output with Pydantic before it touches business logic. Keep temperature low and retry on schema violations with a targeted repair prompt, not a full regeneration.

End of instructions. Please implement now.